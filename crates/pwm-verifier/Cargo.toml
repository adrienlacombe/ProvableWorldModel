# SPDX-License-Identifier: Apache-2.0
[package]
name = "pwm-verifier"
description = "Verify an AuditArtifact: digest public input, check relation_id + commitments, Freivalds-check the linear layers, exactly recompute requant/nonlinear/attention, and check rollout/cost/argmin. Library-first, no_std-clean, float-free, with no dependency on pwm-export or any PyTorch/Python runtime (INV-ARCH-02)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
pwm-core = { workspace = true }

[lints]
workspace = true
