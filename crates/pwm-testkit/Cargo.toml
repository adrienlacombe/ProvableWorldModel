# SPDX-License-Identifier: Apache-2.0
[package]
name = "pwm-testkit"
description = "Shared test scaffolding for ProvableWorldModel: the accept/reject (dual-test) harness, the committed golden-vector loader, and the constraint-mutation runner skeleton. Test infrastructure only; not published."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
# The `pwm` demo CLI (PR-304) drives the full prove -> serialize -> decode ->
# verify path, so it links every first-party crate. Test/tooling only.
pwm-core = { workspace = true }
pwm-export = { workspace = true }
pwm-prover = { workspace = true }
pwm-verifier = { workspace = true }

[lints]
workspace = true
