# SPDX-License-Identifier: Apache-2.0
[package]
name = "pwm-prover"
description = "Prover CLI and orchestration: load manifest, run the integer reference inference, build the execution trace, Merkle-commit it, derive Fiat-Shamir challenges, and emit the AuditArtifact bundle (commit-and-audit; specs.md §6)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
keywords = ["proof-system", "prover", "world-model", "commit-and-audit"]
categories = ["cryptography", "science"]

[dependencies]
pwm-core = { workspace = true }
pwm-export = { workspace = true }

[lints]
workspace = true
